# AI Infinite Canvas 变更分级规则（执行版）

版本：v1.0  
状态：启用  
适用范围：前端、后端、配置、存储相关所有代码改动

## 1) 分级定义（必须先判断）

### 🔴 红色（禁止直接改）
定义：改了可能导致系统运行中断、数据不兼容、请求失败。  
要求：必须先提交变更提案并获得确认后才能修改。

### 🟡 黄色（先确认再改）
定义：改了会改变系统行为或用户结果，但不一定崩。  
要求：先写影响说明 + 回滚方案，确认后再改。

### 🟢 绿色（可直接改）
定义：只影响展示与体验，不改变执行结果与数据结构。  
要求：可直接改，改后自测并记录改动点。

---

## 2) 本项目分级范围

### 🔴 红色范围
1. 执行主链路入口与调度骨架  
2. 前后端请求/响应基础契约  
3. 节点核心数据结构与状态字段  
4. 工作流存档基础结构（导入导出兼容）

### 🟡 黄色范围
1. 自动跳过、失败中断、停止策略  
2. 模型调用策略（文本/图像分支、fallback）  
3. 图像有图输入时的编辑策略（image-edit 规则）  
4. 安全策略（口令、锁定、权限、跨域）  
5. 持久化策略（保留/清理/版本兼容）

### 🟢 绿色范围
1. 页面样式、布局、动画、图标  
2. 按钮文案、日志文案、提示语  
3. 面板交互小优化（不改数据结构）  
4. 非核心展示组件重构

---

## 3) 执行流程（每次改动都走）

1. 先判级（红/黄/绿）  
2. 按级执行  
   - 红：提案 -> 审批 -> 开发 -> 全链路验证  
   - 黄：影响说明 -> 确认 -> 开发 -> 关键路径验证  
   - 绿：直接开发 -> 页面/功能自测  
3. 提交时写清：改动层级、范围、验证结果

---

## 4) 红线（禁止）

1. 未确认直接改红色层  
2. 黄色层不做影响说明就改  
3. 顺手改无关模块（越界修改）  
4. 不验证直接提交主流程  
5. 无回滚预案改核心逻辑

---

## 5) 变更提案模板（红/黄必填）

【变更名称】  
【变更层级】红 / 黄  
【改动目标】  
【改动范围】  
【行为变化】  
【风险点】  
【验证方案】  
【回滚方案】

---

## 6) 提交说明模板（推荐）

【层级】红/黄/绿  
【模块】  
【改动内容】  
【影响范围】  
【验证结果】  
【回滚是否准备】是/否

---

## 7) 一句话判断法（给非技术同学）

- 会不会“跑不起来”？是 -> 红  
- 会不会“行为变了”？是 -> 黄  
- 只是“看起来不一样”？是 -> 绿
